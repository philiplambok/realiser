source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in realiser.gemspec.
gemspec

group :development do
  gem 'sqlite3'
end

# To use a debugger
group :development, :test do
  gem 'byebug'
  gem 'pry', require: true
end
