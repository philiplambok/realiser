<div class="card">
  <div class="card-header">
    Edit Setting #<%= @setting.id %> 
  </div>
  <div class="card-body">
    <%= render 'shared/message' %>

    <%= form_with(model: @setting, url: web_setting_path(@setting), method: :put) do |form| %>
      <div class="form-group row">
        <div class="col-6">
          <div class="mb-3">
            <%= form.label :key, class: 'form-label fw-bold' %>
            <%= form.text_field :key, class: 'form-control', autocomplete: 'off' %>
          </div>
        </div>
        <div class="col-6">
          <div class="mb-3">
            <%= form.label :value, class: 'form-label fw-bold' %>
            <%= form.text_field :value, class: 'form-control', autocomplete: 'off' %>
          </div>
        </div>
      </div>

      <div class="form-group row">
        <div class="col-md-12">
          <%= form.submit 'Update', class: 'btn btn-primary' %>
          <%= link_to 'Back', web_setting_path(@setting), class: 'btn btn-default' %>
        </div>
      </div>
    <% end %>
  </div>
</div>

